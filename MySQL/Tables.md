```sql
# 汇总
SHOW [FULL] TABLES [{FROM | IN} db_name] [LIKE 'pattern' | WHERE expr]

# 展示当前 database 下的所有 tables
show tables;

# 展示当前 database 下符合匹配条件的 tables
show tables [LIKE 'pattern' | WHERE expr]

```
